# 仿真驾驶舱规划

## 文档信息

| 项目 | 内容 |
|---|---|
| 文档类型 | 仿真运行页面、领域模型、交互流程与建设规划 |
| 版本 | v1.0 |
| 日期 | 2026-06-22 |
| 页面路由 | `/simulation` |
| 适用范围 | 单 Workspace 内任务仿真、人工联调、智能体运行、消息追踪、快照与回放 |
| 关联文档 | `多用户与Workspace架构标准_20260622.md`、`通信规范_MQTT_API_20260617.md`、`数据库与数据结构规范_20260617.md` |

## 1. 页面定位

仿真驾驶舱是独立于地图编辑页的运行工作台。

职责：

- 创建和启动 SimulationRun。
- 创建 Task 并观察执行状态。
- 查看 Plan 版本、步骤和重规划原因。
- 查看 Action、MQTT command 和 result event。
- 查看 Observation、CurrentState、Snapshot 和 Trace。
- 在授权模式下触发控制台事件和手动联调 Action。
- 导出单次 Run 的完整联调包。

非职责：

- 不编辑地图正式版本。
- 不直接修改机器人状态。
- 不直接连接 MQTT command Topic。
- 不在前端执行任务规划和 AI 决策。
- 不替代 Platform API、Agent Service 或 Robot Executor。

## 2. 核心模型

建议增加 `SimulationRun` 作为一次运行容器：

```text
Workspace
  └─ SimulationRun
      └─ Task
          └─ Plan v1/v2...
              └─ Action
                  └─ MQTT command
                      └─ MQTT result
                          └─ Observation
                              └─ CurrentState
                                  └─ Snapshot

Trace 贯穿全部对象
```

所有对象必须包含 `workspaceId` 和 `runId`。

## 3. 对象职责

| 对象 | 定义 | 禁止混入的内容 |
|---|---|---|
| Task | 业务目标和完成条件 | 具体设备指令序列 |
| Plan | 实现 Task 的可版本化步骤 | 真实执行结果 |
| Action | 一次可执行尝试 | 多个无关设备动作 |
| Observation | 外部或内部已发生事实 | 下一步决策 |
| CurrentState | Observation 聚合后的当前投影 | 历史日志全集 |
| Snapshot | 指定状态版本的不可变冻结 | 高频实时状态流 |
| Trace | 全链路关联和时间线 | 原始业务 Payload 副本 |

## 4. 标识标准

```text
workspaceId
runId
taskId
planId
planVersion
planStepId
actionId
commandId
requestId
observationId
stateVersion
snapshotId
traceId
spanId
parentSpanId
```

映射规则：

- `taskId`、`commandId`、`requestId`、`traceId` 与机器狗 MQTT 协议保持一致。
- `actionId` 是平台内部执行对象，通常与一个 `commandId` 对应。
- `stop` 创建新 `commandId`，复用目标 Task 的 `taskId` 和 `traceId`。
- `where` 使用 `requestId`；不属于业务任务时 MQTT `taskId=null`。

## 5. 状态机

Task：

```text
Draft → Ready → Running → Succeeded
                    ├→ Paused → Running
                    ├→ Failed
                    └→ Cancelled
```

Plan：

```text
Generating → Ready → Active → Completed
                         ├→ Superseded
                         └→ Failed
```

Action：

```text
Pending → Issued → Accepted → Running → Succeeded
                    ├→ Rejected
                    ├→ Failed
                    ├→ Stopped
                    └→ Timeout
```

Observation：

```text
Received → Validated → Applied
              ├→ Ignored
              └→ Invalid
```

Trace：

```text
Open → Completed
   └→ Error
```

## 6. 对象标准字段

### 6.1 Task

```text
taskId、workspaceId、runId、traceId、goal、input、constraints、priority、
deadline、expectedOutcome、status、createdBy、createdAt、startedAt、finishedAt
```

### 6.2 Plan

```text
planId、taskId、traceId、planVersion、strategy、steps、dependencies、
assumptions、generatedBy、generationLatencyMs、status、createdAt、activatedAt
```

Plan Step：

```text
planStepId、sequence、actionType、target、params、dependsOn、
successCondition、failurePolicy、timeoutMs
```

### 6.3 Action

```text
actionId、planId、planStepId、taskId、traceId、robotCode、command、params、
commandId、requestId、attemptNo、timeoutMs、status、issuedAt、startedAt、finishedAt
```

只有 Action Service 可以将 Action 转换成 MQTT command。

### 6.4 Observation

```text
observationId、workspaceId、runId、taskId、actionId、traceId、source、event、
eventId、robotCode、commandId、requestId、timestamp、data、error、processingStatus
```

Observation 来源：

- MQTT result event。
- 控制台事件。
- 资源状态变化。
- Agent 内部计时器。
- API 集成事件。
- 人工确认。

### 6.5 CurrentState

```text
workspaceId、runId、stateVersion、taskState、activePlan、robotStates、
resourceStates、environmentState、pendingActions、activeEvents、
lastObservationId、lastObservationAt、updatedAt
```

CurrentState 是可重建的派生数据，不是历史事实源。

### 6.6 Snapshot

```text
snapshotId、workspaceId、runId、taskId、traceId、stateVersion、reason、
task、activePlan、actionCursor、robotStates、resourceStates、environmentState、
pendingActions、createdAt、checksum
```

Snapshot 触发时机：

- Run 启动。
- Task 启动和结束。
- Plan 激活或重规划。
- Task 暂停或恢复。
- 严重异常发生。
- 人工创建检查点。
- Run 结束。

禁止按每秒固定频率生成完整 Snapshot。

### 6.7 Trace

Trace Header：

```text
traceId、workspaceId、runId、taskId、status、startedAt、finishedAt、durationMs
```

Trace Span：

```text
spanId、parentSpanId、traceId、entityType、entityId、operation、status、
startedAt、finishedAt、durationMs、inputRef、outputRef、errorRef
```

Trace 只保存引用和关键摘要，原始 MQTT Payload 继续保存在 MessageRecord。

## 7. 标准运行闭环

```text
创建 SimulationRun
→ 绑定地图配置版本和机器人
→ 创建 Task
→ 生成初始 Snapshot
→ Agent 读取 CurrentState
→ 生成 Plan v1
→ 激活 Plan
→ 创建 Action
→ Platform API 校验 Action
→ 发布 MQTT command
→ 机器狗返回 result event
→ Message Hub 创建 Observation
→ Observation 更新 CurrentState
→ Agent 判断继续、重试、停止或生成 Plan v2
→ Task 完成
→ 创建最终 Snapshot
→ 关闭 Trace
```

随机事件和控制台事件必须先创建 Observation，再影响 CurrentState 和 Plan。

## 8. 驾驶舱模式

| 模式 | 用途 | 控制边界 |
|---|---|---|
| 观察模式 | 只看运行状态和 Trace | 不允许创建 Task 或 Action |
| 手动联调模式 | 手工发起 goto_pose、where、stop | 通过 Action API，不直连 MQTT |
| 任务流模式 | 按预定义 Task 模板执行 | Agent 使用规则策略生成 Plan |
| 智能体模式 | AI 或规则智能体动态规划 | Action 必须经过平台校验 |
| 回放模式 | 从 Snapshot 和 Observation 重建过程 | 不发布真实 MQTT command |

## 9. 页面布局

### 9.1 顶部运行栏

- Workspace 与 SimulationRun。
- 地图配置版本。
- 运行状态和持续时间。
- 启动、暂停、恢复、停止。
- 模式选择。
- 时间倍率和随机事件策略。
- 当前 Task、Plan 版本和 Trace ID。

### 9.2 左侧 Task / Plan

- Task 目标、输入、约束、优先级和完成条件。
- Plan 版本选择。
- Plan Step 列表和依赖关系。
- 当前步骤、已完成步骤、失败步骤。
- 重规划原因和旧 Plan 对比。

### 9.3 中央二维 CurrentState

- 当前地图和坐标轴。
- 机器人位置、朝向、轨迹和目标点。
- 工位、障碍物、资源点和路径状态。
- 资源锁和冲突。
- 当前 Action 和异常区域。
- 地图只读；手动联调模式允许选择目标坐标。

### 9.4 右侧 Action / Observation

- Pending、Running、Completed Action。
- commandId、requestId、robotCode、状态和耗时。
- Observation 实时流。
- result event、错误码、重试建议。
- 当前 CurrentState 版本。

### 9.5 底部诊断区

使用 Tabs：

```text
Trace
MQTT Messages
Snapshots
Metrics
Decision Log
Exports
```

## 10. 响应式方案

- 桌面端：地图为主区域，Task/Plan 和 Action/Observation 两侧布局。
- 平板端：地图在上，Task/Plan 与 Action/Observation 使用 Tab。
- 手机端：只提供观察、关键操作和 Trace 摘要，不提供复杂 Plan 编辑。
- 所有面板保持紧凑工作台风格，不使用营销式 Hero 和装饰性卡片。

## 11. 前端设计标准

- Apple + Linear 风格。
- Tailwind、shadcn/ui、Framer Motion。
- 高级 typography、低饱和度、清晰层级。
- 使用图标按钮、Tabs、Segmented Control、Tooltip 和状态 Badge。
- 动画只用于状态切换、列表更新和 Trace 定位，不使用持续性装饰动画。
- 当前状态、错误、等待和离线必须有颜色之外的文字或图标区分。

## 12. API 规划

SimulationRun：

```text
POST /api/v1/simulation-runs
GET  /api/v1/simulation-runs
GET  /api/v1/simulation-runs/{runId}
POST /api/v1/simulation-runs/{runId}/start
POST /api/v1/simulation-runs/{runId}/pause
POST /api/v1/simulation-runs/{runId}/resume
POST /api/v1/simulation-runs/{runId}/stop
```

Task 与 Plan：

```text
POST /api/v1/simulation-runs/{runId}/tasks
GET  /api/v1/tasks/{taskId}
GET  /api/v1/tasks/{taskId}/plans
GET  /api/v1/plans/{planId}
POST /api/v1/tasks/{taskId}/replan
```

Action 与 Observation：

```text
GET  /api/v1/actions
GET  /api/v1/actions/{actionId}
POST /api/v1/actions/{actionId}/stop
GET  /api/v1/observations
GET  /api/v1/current-states/{runId}
```

Snapshot 与 Trace：

```text
POST /api/v1/simulation-runs/{runId}/snapshots
GET  /api/v1/simulation-runs/{runId}/snapshots
GET  /api/v1/snapshots/{snapshotId}
GET  /api/v1/traces/{traceId}
GET  /api/v1/traces/{traceId}/spans
GET  /api/v1/simulation-runs/{runId}/export
```

现有 `/api/v1/commands` 作为 Action Service 的底层设备指令接口保留。

## 13. WebSocket 推送

驾驶舱订阅：

```text
/ws/v1/workspaces/{workspaceId}/runs/{runId}
```

推送类型：

```text
run.updated
task.updated
plan.activated
action.updated
observation.created
current_state.updated
snapshot.created
trace.span.created
robot.pose.updated
```

WebSocket 只用于展示；所有写操作必须通过 REST API。

## 14. Workspace 隔离

- 驾驶舱只能访问当前 JWT `workspaceId`。
- 页面路由、API、WebSocket、数据库和 MQTT Broker 均属于当前 Workspace。
- 一个 Workspace 的 Snapshot、Trace、日志和导出文件不得被另一个 Workspace 查询。
- 用户切换 Workspace 时必须重新加载连接信息和运行上下文。
- 前端不得缓存其他 Workspace 的 CurrentState。

## 15. 数据一致性

- Observation 和 MessageRecord 采用追加写入。
- 使用 `eventId/messageId` 去重。
- Action 使用 `commandId` 幂等。
- CurrentState 按 `stateVersion` 乐观更新。
- 同机器人 Observation 按设备时间和平台接收时间处理乱序。
- 延迟 result 必须记录，但不得覆盖较新的 CurrentState。
- Snapshot 必须引用明确的 `stateVersion`。
- Plan 重规划必须创建新版本，旧版本不可覆盖。

## 16. 指标

- Task 完成率和平均耗时。
- Plan 重规划次数。
- Action 成功率、拒绝率、超时率和重试次数。
- Observation 延迟和无效消息数。
- CurrentState 更新延迟。
- Snapshot 数量和存储量。
- Trace 完整率。
- 机器人利用率、资源等待时间和异常次数。

## 17. 导出包

单次 Run 导出包包含：

```text
manifest.json
workspace.json
run.json
task.json
plans.json
actions.json
observations.json
current-state-final.json
snapshots/
trace.json
mqtt-messages.json
metrics.json
audit.json
```

导出包不得包含 MQTT 密码、数据库连接串、JWT 和证书私钥。

## 18. 建设阶段

### C0：只读驾驶舱

- Run 列表。
- 当前地图和机器人状态。
- MQTT 消息、Trace 和健康状态。

### C1：Task / Plan / Action

- 创建 Task。
- 展示 Plan 版本和步骤。
- 手动联调 Action。
- Action 与 command/result 串联。

### C2：Observation / CurrentState

- result event 标准化为 Observation。
- CurrentState 聚合和版本管理。
- 控制台事件进入 Observation。

### C3：Snapshot / Trace / 回放

- 检查点 Snapshot。
- Trace Header 和 Span。
- 单次 Run 导出。
- 基于 Snapshot 的只读回放。

### C4：Agent 与复杂仿真

- 规则 Agent。
- AI Agent 适配。
- 重规划、资源锁、随机事件和多机器人。

## 19. 验收标准

- 驾驶舱与地图编辑页面职责分离。
- 用户只能访问授权 Workspace 的驾驶舱。
- Task、Plan、Action、Observation、CurrentState、Snapshot、Trace 可以完整串联。
- Action 可追踪到 MQTT command 和全部 result event。
- result event 可生成 Observation 并更新 CurrentState。
- Plan 重规划产生新版本且旧版本可查询。
- Snapshot 可恢复指定检查点的只读状态。
- Trace 可说明任务为何成功、失败、等待或重规划。
- WebSocket 断开后可回退 REST 查询。
- 单次 Run 可导出完整且不包含 Secret 的联调包。
